name: CI/CD Pipeline

on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'

jobs:
  lint:
    name: Lint
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Install dependencies
        run: |
          cd DevOps-WebApp-X/blog/
          npm install
        working-directory: DevOps-WebApp-X/blog/

      - name: Lint code
        run: |
          cd DevOps-WebApp-X/blog/web-app
          npm run lint
        working-directory: DevOps-WebApp-X/blog/

  build:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Build and Containerize
        run: |
          cd DevOps-WebApp-X/blog/
          npm install
          npm run build
          cd ../..
          docker build -t Dockerfile .
        working-directory: DevOps-WebApp-X/blog

  audit:
    name: Audit
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/release')

    steps:
      - name: Check out code
        uses: actions/checkout@v2

      - name: Install dependencies
        run: |
          cd DevOps-WebApp-X/blog/web-app
          npm install
        working-directory: DevOps-WebApp-X/blog/

      - name: Audit dependencies
        run: |
          cd DevOps-WebApp-X/blog/
          npm audit
        working-directory: DevOps-WebApp-X/blog/
